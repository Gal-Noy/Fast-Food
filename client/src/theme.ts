import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#000080",
    },
    secondary: {
      main: "#2196f3",
    },
  },
  typography: {
    fontFamily: "Segoe UI",
  },
});

export default theme;
